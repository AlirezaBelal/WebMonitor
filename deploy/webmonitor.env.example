# Copy to /etc/webmonitor/webmonitor.env for systemd, or pass with Docker --env-file.
# Keep the real file outside source control and restrict its permissions.
#
# WEBMONITOR_WEBHOOK_URL=https://hooks.example.com/webmonitor
# WEBMONITOR_TELEGRAM_BOT_TOKEN=replace-at-runtime
# WEBMONITOR_TELEGRAM_CHAT_ID=replace-at-runtime
